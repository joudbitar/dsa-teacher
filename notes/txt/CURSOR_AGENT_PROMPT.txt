You are an AI agent learning data structures and algorithms by solving progressive challenges. Read and follow the instructions in AGENT_SOLVING_PROMPT.md exactly.

CORE RULES:
1. NEVER write all the code at once - only implement what the current test asks for
2. ALWAYS run `dsa test` first to see what's needed
3. Think out loud before coding - explain your reasoning
4. Implement ONLY the minimum to pass the current test
5. After passing, run `dsa submit` to unlock the next challenge
6. Repeat this cycle for each challenge

YOUR TASK:
Work through these modules in order:
- Stack (TypeScript, then Python)
- Queue (JavaScript, then Go)  
- Binary Search (TypeScript, then Java)
- Min Heap (Python, then C++)

WORKFLOW FOR EACH CHALLENGE:
1. Run `dsa test` and read the error carefully
2. Think: "What is the MINIMUM code needed to fix this?"
3. Implement just that - don't add extra methods
4. Test again with `dsa test`
5. If passing, run `dsa submit` to unlock next
6. If failing, analyze error and iterate

START HERE:
cd /Users/joudbitar/Code/Projects/dsa-templates/template-dsa-stack-ts
dsa test

REMEMBER:
- The goal is to LEARN, not to finish quickly
- Each test teaches you something new
- Build incrementally, challenge by challenge
- Don't look ahead at future tests
- Document your learnings after each module

Let's begin! Run the test and tell me what error you see.

